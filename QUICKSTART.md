# 快速开始指南

## 最快5分钟上手

### 1. 一键安装
```bash
chmod +x setup.sh
./setup.sh
```

脚本会自动：
- 检测系统环境
- 安装C和Python依赖
- 编译游戏引擎
- 创建必要目录

### 2. 立即开始训练

**方式一：纯文本模式（推荐）**
```bash
make train
```

**方式二：可视化模式**
```bash
make train-vis
```

就这么简单！系统会开始训练AI坦克。

### 3. 观察训练进度

纯文本模式会显示：
```
================================================================================
回合   123 | WIN  | 奖励:  145.32 | 步数:  567 | 耗时: 5.23s
================================================================================
  平均奖励 (100回合):  128.45 | 最佳奖励:  198.76
  平均损失 (100步):      0.0234
  探索率 (Epsilon):      0.8234
  训练步数:              12345
  经验缓冲区:            10000 / 100000
  胜/负/平:               45 /   78 /    0 (胜率: 36.6%)
  当前敌人数量:           3
  连胜次数:               2
  历史版本数:             0
================================================================================
```

### 4. 玩家对战

训练一段时间后，可以自己与AI对战：
```bash
make run-player
```

控制：
- `W/A/S/D` - 移动
- `方向键` - 射击

## 常用命令

```bash
# 编译
make all              # 编译所有
make clean            # 清理
make rebuild          # 重新编译

# 训练
make train            # 纯文本训练
make train-vis        # 可视化训练
make train-continue   # 继续训练

# 玩家模式
make run-player       # 与AI对战
```

## 训练参数调整

编辑 `python/config.py`：

```python
# 调整初始难度
ENV_CONFIG = {
    'initial_enemies': 1,  # 改为1个敌人（更容易）
}

# 调整学习率
MODEL_CONFIG = {
    'learning_rate': 0.0002,  # 增加学习率
}

# 可视化设置
TRAINING_CONFIG = {
    'visualize': True,  # 改为True启用可视化
}
```

## 下一步

- 阅读 [README.md](README.md) 了解详细功能
- 查看 [python/config.py](python/config.py) 了解所有配置选项
- 监控 `saved_models/` 目录查看保存的模型

## 常见问题

**Q: 训练太慢？**
- 使用纯文本模式 `--no-visualize`
- 确保使用GPU（检查 `torch.cuda.is_available()`）

**Q: AI一直输？**
- 降低初始敌人数量到1
- 延长训练时间（至少1000回合）

**Q: 想看训练过程？**
- 使用可视化模式 `make train-vis`

祝你训练愉快！🎮🤖
